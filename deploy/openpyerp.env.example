# /etc/openpyerp.env
# Bu dosyayı sunucuya koy, izinleri kısıtla:
#   sudo chmod 640 /etc/openpyerp.env
#   sudo chown root:www-data /etc/openpyerp.env
#
# ASLA git'e commit etme!

SECRET_KEY=buraya_en_az_32_karakter_rastgele_bir_string_yaz

DATABASE_URL=postgresql+psycopg2://openpyerp_user:sifren@localhost/openpyerp

# Flask ortamı
FLASK_ENV=production
